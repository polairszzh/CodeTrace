# API Key 统一鉴权（与后端 CODETRACE_API_KEY 保持一致）
# 部署时在构建环境设置；本地开发留空即可
# 注意：该 key 会打包进前端产物、任何访问者都可读取，仅作轻量访问控制，
# 真正的安全边界应依赖网络层防护（防火墙/VPN/反向代理鉴权），不要把机密 key 放这里
VITE_CODETRACE_API_KEY=

# API 基础路径（默认同源 /api；跨域部署时设为完整后端地址，如 https://api.example.com/api，
# 相对 /api 请求会自动改写为该基址并注入 key）
# 不要设为 /（根路径）：该配置会匹配所有同源请求，为避免 key 外泄，程序会跳过注入并在控制台告警
VITE_CODETRACE_API_BASE=/api
